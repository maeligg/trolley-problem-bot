# Trolley problem bot

This bot generates a random trolley problem image based on a bank of images and posts it to Twitter.

# Usage

To test the bot locally, you will need to set environment variables for the following API keys : 'consumer_key', 'consumer_secret', 'access_token', 'access_token_secret'.

# Contribute

Create an issue if you'd like to suggest adding a particular image.